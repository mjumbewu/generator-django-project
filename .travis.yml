language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-django-project' ]; then cd .. && eval "mv $currentfolder generator-django-project" && cd generator-django-project; fi

